@model Someren.Models.Lecturer
@{
    ViewData["Title"] = "Add Lecturer";
}

<div class="card mb-4 fade-in">
    <div class="card-header">
        <h1 class="card-title">Add New Lecturer</h1>
        <a href="/Lecturers/Index" class="btn btn-primary shadow-button">
            <i class="fas fa-arrow-left me-2"></i> Back to List
        </a>
    </div>
    <div class="card-body">
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success mx-0 mb-4">
                <i class="fas fa-check-circle me-2"></i> @TempData["SuccessMessage"]
            </div>
        }

        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger mx-0 mb-4">
                <i class="fas fa-exclamation-circle me-2"></i> @TempData["ErrorMessage"]
            </div>
        }

        @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
        {
            <div class="alert alert-danger mx-0 mb-4">
                <i class="fas fa-exclamation-circle me-2"></i> @ViewBag.ErrorMessage
            </div>
        }

        <form action="/Lecturers/Create" method="Post">
            <div class="mb-3">
                <label for="LecturerId" class="form-label">Lecturer ID</label>
                <input type="number" id="LecturerId" name="LecturerId" class="form-control" required placeholder="12345" value="@Model?.LecturerId" />
            </div>

            <div class="mb-3">
                <label for="FirstName" class="form-label">First Name</label>
                <input type="text" id="FirstName" name="FirstName" class="form-control" required placeholder="John" value="@Model?.FirstName" />
            </div>

            <div class="mb-3">
                <label for="LastName" class="form-label">Last Name</label>
                <input type="text" id="LastName" name="LastName" class="form-control" required placeholder="Doe" value="@Model?.LastName" />
            </div>

            <div class="mb-3">
                <label for="RoomNumber" class="form-label">Room Number</label>
                <input type="text" id="RoomNumber" name="RoomNumber" class="form-control" required placeholder="A1-01" value="@Model?.RoomNumber" />
            </div>

            <div class="mb-3">
                <label for="PhoneNumber" class="form-label">Telephone Number</label>
                <input type="tel" id="PhoneNumber" name="PhoneNumber" class="form-control" required placeholder="0612345678" value="@Model?.PhoneNumber" />
            </div>

            <div class="mb-3">
                <label for="Age" class="form-label">Age</label>
                <input type="number" min="18" max="120" id="Age" name="Age" class="form-control" required placeholder="32" value="@Model?.Age" />
            </div>

            <p>Does this lecturer have bar duty?</p>
            <input type="radio" id="BarDuty" name="BarDuty" required value="True" />
            <label for="BarDuty">True</label>

            <input type="radio" id="BarDuty" name="BarDuty" required value="False" />
            <label for="BarDuty">False</label> <br />

            <div class="d-flex justify-content-center mt-4">
                <button type="submit" class="btn btn-primary shadow-button">
                    <i class="fas fa-plus-circle me-2"></i> Add Lecturer
                </button>
            </div>
        </form>
    </div>
</div>
