@model Someren.Models.Activity
@{
    ViewData["Title"] = "Restore Activity";
}

<div class="card mb-4 fade-in">
    <div class="card-header">
        <h1 class="card-title">Restore Activity</h1>
        <a asp-action="ListDeleted" class="btn btn-primary shadow-button">
            <i class="fas fa-arrow-left me-2"></i> Back to List
        </a>
    </div>
    <div class="card-body">
        <div class="alert alert-warning">
            <i class="fas fa-exclamation-triangle me-2"></i> Are you sure you want to restore this activity?
        </div>

        <div class="card mb-4 border">
            <div class="card-header bg-light">
                <h5 class="mb-0">Activity Details</h5>
            </div>
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-md-4 fw-bold">
                        <i class="fas fa-tag me-2"></i> Activity Name:
                    </div>
                    <div class="col-md-8">
                        @Model.Activitytype
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-4 fw-bold">
                        <i class="fas fa-calendar me-2"></i> Date:
                    </div>
                    <div class="col-md-8">
                        @Model.Date.ToShortDateString()
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-4 fw-bold">
                        <i class="fas fa-clock me-2"></i> Time:
                    </div>
                    <div class="col-md-8">
                        @Model.Time
                    </div>
                </div>
            </div>
        </div>

        <form asp-action="Restore" method="post">
            <input type="hidden" name="id" value="@Model.Activitytype" />
            <input type="hidden" name="confirmed" value="true" />
            <div class="d-flex justify-content-center">
                <button type="submit" class="btn btn-success shadow-button">
                    <i class="fas fa-undo me-2"></i> Restore Activity
                </button>
            </div>
        </form>
    </div>
</div>